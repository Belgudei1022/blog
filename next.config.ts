/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com"], // 🟢 Google-н зурагны hostname-г зөвшөөрөх
  },
};

module.exports = nextConfig;
